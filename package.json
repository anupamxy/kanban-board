{
  "name": "kanban-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --include=dev --prefix backend && npm run build --prefix backend && mkdir -p backend/dist/public && cp -r frontend/dist/. backend/dist/public",
    "start": "node backend/dist/index.js"
  }
}
